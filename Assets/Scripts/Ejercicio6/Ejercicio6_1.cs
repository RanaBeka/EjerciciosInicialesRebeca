using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        ///Lo que está dentro del parentesis tiene que cumplirse una de las dos
        ///a)(X=true y Y=false) o (X=true y Z=true)
        ///b)(X=true o Y=true) y (X=false o z=true)
        ///c)X=true o Y=false y Z=true
        ///d)(X=false o Y=true) y Z=true
        ///e)X=true o Y=false o X=true y Z=false y Y=true
        ///f)X=false o Y=true o Z=ture y X=true y Y=true
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
